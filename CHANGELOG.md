# Changelog

This project adheres to Semantic Versioning.

## [0.7.21] - 2026-06-02

### Fixed
- Apple 미리알림 자동 동기화를 비활성화해 할 일/메모/습관 추가나 여러 줄 붙여넣기 중 `add_to_reminders`가 반복 호출되며 권한 창이 계속 뜨는 문제를 막았습니다.
- 수동 전체 동기화 중 권한 실패가 한 번 발생하면 같은 앱 실행 세션에서는 나머지 미리알림 쓰기 요청을 중단하도록 변경했습니다.

## [0.7.20] - 2026-06-02

### Fixed
- Apple 미리알림 목록 조회를 앱 실행 중 한 번만 요청하도록 캐시해 설정 화면 재진입이나 토글 중복 처리로 권한 허용 창이 반복 표시되는 문제를 줄였습니다.
- macOS 번들 Info.plist에 Reminders/Apple Events 권한 설명을 포함해 권한 허용이 앱 단위로 정상 기록되도록 보강했습니다.

## [0.7.10] - 2026-06-01

### Added
- **Tauri 창 최소 가로 너비 확장**: `tauri.conf.json`의 `minWidth` 제한을 380px에서 420px로 상향하여, 창 크기를 최소로 줄였을 때 사이드 탭 버튼이 화면 왼쪽 바깥으로 잘리거나 본체와 겹치던 레이아웃 깨짐 버그를 방지했습니다.

### Fixed
- **주간 플래너 뷰 높이 꽉 차게 복원**: `WeekPane` 최상위를 Fragment에서 실제 flex column `div`로 감싸서 브라우저 높이 상속 유실 문제를 해결했습니다. 이로 인해 주간 격자가 아래쪽 빈 여백 없이 세로 전체 크기로 균등하게 꽉 차도록 사이즈가 정상 조정되었습니다. (노트 가로줄 배경 및 팀플 데이터 연동 정상 유지)
- **주간 플래너 2열 격자 가로 비율 균등 분할**: `gridTemplateColumns: "1fr 1fr"`을 `repeat(2, minmax(0, 1fr))`로 변경하여, 긴 할 일 텍스트 입력 시에도 정확히 50% 대 50%의 균등 가로폭 대칭 분할이 보장되도록 수정했습니다.
- **상단 타이머(WorkTime) 동작 정지/재개 클릭 기능 복원**: 인디케이터 점 버튼의 작동 정지/재개 클릭(WORKING ↔ STOPPED) 및 상태 연동 로직을 정상 복구했습니다. (불필요한 리셋 ⟳ 버튼 삭제)
- **사이드 탭 버튼 정렬 라인 일관화**: 활성 탭이 가로로 넓어져 왼쪽 정렬 라인이 깨지던 현상을 제거하고 모든 탭 버튼의 가로 너비(32px)와 마진(0)을 동일하게 고정하여 정렬 라인을 완벽하게 일치시켰습니다.
- **음악 볼륨 실시간 제어**: 비디오의 동적 볼륨 프로퍼티를 직접 감시하게 변경하여 볼륨 조절 슬라이더 조작 시 실시간으로 즉시 음악 소리에 반영됩니다.
- **미니 위젯 텍스트 가운데 정렬 및 클릭 최대화**: 우측 상단 최대화 버튼(`⤢`)을 제거하여 시간 텍스트가 정중앙에 정렬되도록 조정하였으며, 시간 텍스트를 클릭하면 미니화가 즉시 해제(도크 복원)되도록 개선했습니다.
- **투두 마감일 배지 복구**: 📅 달력 아이콘으로 덮어씌워졌던 마감일 지정 인터페이스를 기존의 숫자 뱃지(예: `6/1`)를 클릭해 동작하는 원래의 직관적인 디자인 방식으로 롤백했습니다.
- **백업/복원 tweaks 연동**: 데이터 내보내기/가져오기 시 포인트 컬러 및 데스크탑 투명화 등 tweaks 로컬스토리지 데이터를 JSON 백업 파일에 통합하여 함께 완벽 복원되도록 연동했습니다.
- **유튜브 플레이리스트 외부 크게 보기 버튼 복구**: 플레이리스트 내부 유튜브 영역에 외부 브라우저 YouTube 링크 열기 `↗ 크게 보기` 버튼을 복원했습니다.
- **설정 내 뽀모도로 온오프 버튼 제거**: 더 이상 사용하지 않는 설정 내 뽀모도로 활성 토글을 제거하고 항상 구동(`showPomodoro = true`)되도록 고정했습니다.
